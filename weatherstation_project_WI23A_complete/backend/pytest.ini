[pytest]
markers =
    unit: fast, isolated tests without external data dependencies
    integration: tests that exercise real service, API, or repository integration paths
    slow: tests that are noticeably slower due to startup, I/O, or larger datasets
